apiVersion: v1
kind: Service
metadata: 
  name: accounts-srv
spec:
  selector:
    app: accounts
  type: ClusterIP
  ports:  
  - name: accounts-port
    port: 7765

# ---

# apiVersion: v1
# kind: Service
# metadata: 
#   name: mailer-srv
# spec:
#   selector:
#     app: mailer
#   type: ClusterIP
#   ports:  
#   - name: mailer-port
#     port: 7765